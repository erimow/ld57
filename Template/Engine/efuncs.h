#ifndef efuncs
#define efuncs

extern int divideUp(int numerator, int denominator); //Divides an int but rounds up.
extern float maxf(float number, float maxValue); //If number exceeds max, then set to max.

#endif
